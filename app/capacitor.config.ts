import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "network.oranfify.app",
  appName: "Orangify",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
